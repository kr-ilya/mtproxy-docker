# MTProxy Configuration
# Copy this file to .env and customize

# Proxy secret (32 hex characters)
# Leave empty to auto-generate on first run
SECRET=

# ============================================
# Optional Settings  
# ============================================

# Tag from @MTProxybot for statistics
TAG=

# Connection port (default:  443)
PORT=8744

# Multiple ports (comma-separated, overrides PORT)
# PORTS=443,8443

# Number of workers (default: auto-detect based on CPU)
# WORKERS=2

# Statistics port (default: 8888)
STATS_PORT=8888

# External IP address
# Leave empty for auto-detection
EXTERNAL_IP=

# MTProxy repository URL
MTPROTO_REPO_URL=https://github.com/TelegramMessenger/MTProxy

# Specific commit/tag to build
MTPROTO_COMMIT=cafc338

# Config auto-update interval (seconds)
# Default: 7 days
CONFIG_UPDATE_INTERVAL=604800

